---
import { getCollection } from "astro:content";
import Container from "@components/Container.astro";
import PageLayout from "@layouts/PageLayout.astro";
import Link from "@components/Link.astro";
import { dateRange, groupWorkByCompany } from "@lib/utils";
import { socialIcons } from "@lib/icons";
import { SITE, HOME, SOCIALS } from "@consts";
import { getLangFromUrl, useTranslations } from "@i18n/ui";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

// Use appropriate collection based on language
const workCollection = lang === "pt" ? "work-pt" : "work";
const allwork = (await getCollection(workCollection))
  .sort((a, b) => new Date(b.data.dateStart).valueOf() - new Date(a.data.dateStart).valueOf());

const work = await Promise.all(
  allwork.map(async (item) => {
    const { Content } = await item.render();
    return { ...item, Content };
  })
);

const companies = groupWorkByCompany(work).slice(0, SITE.NUM_WORKS_ON_HOMEPAGE);
const langPrefix = lang === "pt" ? "/pt" : "";
---

<PageLayout title={HOME.TITLE} description={HOME.DESCRIPTION}>
  <Container>
    <div class="flex flex-col sm:flex-row gap-6 items-center sm:items-center mb-8">
      <div class="flex-1 text-center sm:text-left">
        <h4 class="animate font-semibold text-black dark:text-white text-2xl sm:text-3xl">
          {t("home.greeting")} <span class="text-xl">👋🏻</span>
        </h4>
      </div>
    </div>
    <div class="space-y-16">
      <section>
        <article class="space-y-4">
          <p class="animate">
            {t("home.intro1")}
          </p>
          <p class="animate">
            {t("home.intro2")}
          </p>
          <p class="animate" set:html={t("home.intro3")} />
          <div id="catb-embed" class="mt-4 grid grid-rows-[0fr] opacity-0 transition-all duration-500 ease-out">
            <div class="overflow-hidden">
              <iframe 
                style="border-radius:12px" 
                src="https://open.spotify.com/embed/artist/2xaAOVImG2O6lURwqperlD?utm_source=generator&theme=0" 
                width="100%" 
                height="152" 
                frameborder="0" 
                allowfullscreen 
                allow="autoplay; clipboard-write; encrypted-media; fullscreen; picture-in-picture" 
                loading="lazy"
              ></iframe>
            </div>
          </div>
        </article>
      </section>

      <section class="animate space-y-6">
        <div class="flex flex-wrap gap-y-2 items-center justify-between">
          <h5 class="font-semibold text-black dark:text-white">
            {t("home.career.title")}
          </h5>
          <Link href={`${langPrefix}/work`}>
            {t("home.career.seeAll")}
          </Link>
        </div>
        <ul class="flex flex-col space-y-6">
          {companies.map(({ company, roles, dateStart, dateEnd, hasMultipleRoles }) => (
            <li>
              <div class="mb-2">
                <div class="font-semibold text-black dark:text-white">
                  {company}
                </div>
                {hasMultipleRoles && (
                  <div class="text-sm opacity-75">
                    {dateRange(dateStart, dateEnd, lang)}
                  </div>
                )}
              </div>
              <ul class={hasMultipleRoles ? "ml-4 space-y-3 border-l-2 border-gray-300 dark:border-gray-700 pl-4" : "space-y-3"}>
                {roles.map(entry => (
                  <li>
                    <div class="text-sm opacity-75">
                      {dateRange(entry.data.dateStart, entry.data.dateEnd, lang)}
                    </div>
                    <div class={hasMultipleRoles ? "font-medium text-black dark:text-white text-sm" : "text-sm opacity-75"}>
                      {entry.data.role}
                    </div>
                    <article>
                      <entry.Content />
                    </article>
                  </li>
                ))}
              </ul>
            </li>
          ))}
        </ul>
      </section>

      <section class="animate space-y-4">
        <h5 class="font-semibold text-black dark:text-white">
          {t("home.connect.title")}
        </h5>
        <div>
          <p>
            {t("home.connect.text")}
          </p>
        </div>
        <ul class="flex flex-wrap gap-4 pt-2">
          {SOCIALS.map(social => (
            <li>
              <a 
                href={social.HREF} 
                target="_blank"
                rel="noopener noreferrer"
                aria-label={`${SITE.NAME} on ${social.NAME}`}
                class="flex items-center justify-center w-10 h-10 rounded-full bg-zinc-100 dark:bg-zinc-800 hover:bg-zinc-200 dark:hover:bg-zinc-700 transition-all hover:scale-110"
              >
                <span class="text-zinc-600 dark:text-zinc-400" set:html={socialIcons[social.NAME]} />
              </a>
            </li>
          ))}
          <li>
            <a 
              href={`mailto:${SITE.EMAIL}`}
              aria-label={`Email ${SITE.NAME}`}
              class="flex items-center justify-center w-10 h-10 rounded-full bg-zinc-100 dark:bg-zinc-800 hover:bg-zinc-200 dark:hover:bg-zinc-700 transition-all hover:scale-110"
            >
              <span class="text-zinc-600 dark:text-zinc-400" set:html={socialIcons.email} />
            </a>
          </li>
        </ul>
      </section>
    </div>
  </Container>
</PageLayout>
